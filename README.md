# azure-netflix-data-pipeline
End-to-end Azure data pipeline that ingests Netflix catalog data via ADF, processes it through Bronze/Silver/Gold layers on Databricks, and applies data quality checks with Delta Live Tables.
